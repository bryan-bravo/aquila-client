import { TimelineBarDirective } from './timeline-bar.directive';

describe('TimelineBarDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineBarDirective();
    expect(directive).toBeTruthy();
  });
});
